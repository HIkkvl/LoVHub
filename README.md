📚 Документация по Системе Управления "LoVHub"
Это руководство поможет вам развернуть и настроить серверную и клиентскую части вашей системы.

1. Обзор Системы
Ваша система состоит из трех основных частей:

Сервер (server.py): "Мозг" системы. Это Flask-приложение, которое хранит базу данных (пользователи, ПК, тарифы, логи), управляет сессиями и предоставляет API для клиентов. Он должен быть запущен 24/7 на вашем главном компьютере или выделенном сервере.

Клиент (Лаунчер): Набор Python-скриптов (auth.py, main_window.py, watchdog.py и др.). Он устанавливается на каждый игровой ПК. Его задача — блокировать компьютер до входа, отсчитывать время и отправлять "пульс" (heartbeat) на сервер.

Веб-Админка (HTML/CSS/JS): Ваш "пульт управления". Это веб-сайт, который вы открываете в браузере (Chrome, Edge), чтобы видеть, что происходит в клубе, управлять клиентами, тарифами и приложениями.

2. Настройка Сервера (Ваш главный компьютер)
Это нужно сделать один раз на том компьютере, который будет вашим сервером.

Шаг 2.1: Установка библиотек
Серверу нужны Python-библиотеки. Создайте файл requirements_server.txt в папке сервера, скопируйте в него этот текст:

Plaintext

flask
flask-cors
flask-wtf
flask-login
flask-basicauth
requests
werkzeug
...и выполните в терминале (в папке сервера): pip install -r requirements_server.txt

Шаг 2.2: Конфигурация (config.ini)
В корневой папке сервера (рядом с server.py) создайте файл config.ini. Это самый важный файл.

Ini, TOML

[Admin]
AdminUsername = ваш_админ_логин
AdminPassword = ваш_сложный_админ_пароль

[Flask]
SecretKey = сгенерируйте_длинный_случайный_ключ_для_сессий

[Kaspi]
PublicKey = ВАШ_KASPI_PUBLIC_KEY_(когда_получите)
PrivateKey = ВАШ_KASPI_PRIVATE_KEY_(когда_получите)
AdminUsername / AdminPassword: Используется для входа в Веб-Админку, а также для админ-действий в лаунчере (добавление игр, пополнение баланса).

SecretKey: Введите сюда любую длинную случайную строку (например, kj234_my-secret-key_!@#).

Шаг 2.3: SSL-Сертификаты (HTTPS)
Ваш сервер работает по HTTPS. Вам нужны файлы cert.pem и key.pem.

Для теста (Самоподписанный): Вы можете сгенерировать их сами. Если у вас установлен openssl, выполните в терминале эту команду (она создаст оба файла):

Bash

openssl req -x509 -newkey rsa:4096 -nodes -out cert.pem -keyout key.pem -days 365
Для "Боевого" режима (Kaspi): Когда будете подключать Kaspi, вам понадобится настоящий SSL-сертификат, купленный для вашего доменного имени.

Шаг 2.4: Запуск Сервера
Поместите cert.pem и key.pem в папку с server.py.

Bash

py server.py
Если все настроено правильно, вы увидите: Запуск сервера в режиме HTTPS... Сервер будет доступен по адресу https://[IP_АДРЕС_СЕРВЕРА]:5000.

3. Настройка Клиента (Игровые ПК)
Это нужно сделать на каждом игровом компьютере.

Шаг 3.1: Установка библиотек
Клиенту нужны свои библиотеки. Создайте requirements_client.txt, скопируйте в него этот текст:

Plaintext

PyQt5
requests
psutil
keyboard
pywin32
...и выполните в терминале: pip install -r requirements_client.txt

Шаг 3.2: Настройка IP-адреса (Критически Важно!)
Клиент должен знать, где находится ваш сервер. IP-адрес сервера "зашит" в коде.

Узнайте статический IP-адрес вашего сервера в локальной сети (например, 192.168.100.15).

Откройте auth.py (Скрипт 2) и utils/workers.py (Скрипт 4).

Используйте "Найти и Заменить" (Ctrl+H) во всех файлах, чтобы заменить https://192.168.100.15:5000 на https://[ВАШ_НОВЫЙ_IP]:5000.

Ваш клиент использует verify=False (в auth.py и workers.py), поэтому он будет работать с вашим самоподписанным SSL-сертификатом.

Шаг 3.3: Конфигурация (config.ini)
Скопируйте тот же config.ini, который вы создали для сервера, в корневую папку клиента.

Клиенту он нужен для:

Админ-действий: AddAppWorker и TopUpBalanceWorker (которые мы исправили) будут брать AdminUsername и AdminPassword из этого файла, когда вы вводите пароль в лаунчере.

utils/config_loader.py: Убедитесь, что этот файл (config_loader.py) тоже скопирован в папку utils на клиенте.

Шаг 3.4: Настройка Автозапуска
Нажмите Win + R.

Введите shell:startup и нажмите Enter (откроется папка автозагрузки).

Создайте в папке с клиентом ярлык для файла auth.py.

Переместите этот ярлык в папку "Автозагрузка".

Теперь, при включении ПК, будет запускаться auth.py (экран входа), который блокирует систему.

4. Использование Веб-Админки
4.1. Вход
Откройте в браузере https://[IP_АДРЕС_СЕРВЕРА]:5000/admin/login. Используйте AdminUsername и AdminPassword из config.ini.

4.2. Дашборд
Статус-бар: Показывает общее/включенное/отключенное (ошибки) количество ПК. "Отключен" означает, что "пульс" (heartbeat) не приходил больше 60 секунд.

Ключевые метрики: Статистика по клиентам и играм.

Сводка по выручке: Считает доход за сегодня (с 00:00).

Проблемные ПК: Список всех ПК со статусом "Отключен".

4.3. Компьютеры
Это "живая" страница. Она обновляется каждые 5 секунд автоматически.

№: Имя ПК. Изначально это Hostname (напр., DESKTOP-J84HGA).

Переименовать: Нажмите кнопку, чтобы задать псевдоним (напр., ПК-1). Этот псевдоним будет отображаться здесь, в Дашборде и в Логах.

Статус:

Отключен: (Красный) "Пульс" не приходил > 60 сек.

Активен: (Оранжевый) "Пульс" есть, но пользователь не в сессии (время 0).

Используется: (Оранжевый) "Пульс" есть, время тикает.

4.4. Клиенты
Зарегистрировать клиента: Кнопка вверху, открывает новую страницу регистрации.

Пополнить баланс / Добавить время: Формы прямо в таблице. Это административное действие (вычитает из ниоткуда).

Удалить: Удаляет пользователя (админа удалить нельзя).

4.5. Тарифы
Кнопка "+": Открывает модальное окно для создания тарифа.

Кнопки (карандаш/корзина): Редактирование и удаление тарифов.

4.6. Приложения
Вкладки: Сортировка по "Игры" / "Приложения" (вы указываете это при создании).

Кнопка "+": Открывает модальное окно (дизайн по вашему макету).

Кнопка (карандаш): Редактирование (также во всплывающем окне).

Внимание: Поле "Путь к .exe" должно содержать абсолютный путь к файлу на клиентском компьютере (например, C:\Games\Dota 2\dota2.exe).

4.7. Логи
Показывает таблицу запусков приложений с псевдонимами ПК и логинами пользователей.

5. Подключение Kaspi Pay
Получите ключи и документацию API от Kaspi Pay.

Внесите ключи в config.ini.

Откройте server.py.

Замените константы KASPI_API_URL и KASPI_SIGNATURE_HEADER на реальные значения из документации.

В функции create_payment(): замените код "ЗАГЛУШКА" на реальный requests.post() к API Kaspi для создания счета.

В функции payment_webhook(): Убедитесь, что create_kaspi_signature() работает именно так, как требует Kaspi (они могут использовать другой порядок полей или другой метод подписи).

Проброс портов: Ваш сервер (порт 5000) должен быть доступен из внешнего интернета, чтобы Kaspi мог отправить вам "вебхук".
